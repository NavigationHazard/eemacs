export default function FileView(){
return(


      <div className="px-3  bg-white h-full overflow-y-scroll ">
    <div>  (defsubst hash-table-empty-p (hash-table) "Check whether HASH-TABLE is empty (has 0 elements)."
(zerop (hash-table-count hash-table)))
Q
(defsubst hash-table-keys (hash-table) "Return a list of keys in HASH-TABLE."
(let (kevs '(1))
(maphash (lambda (k V) (push k keys) ) hash-table)
keys))
(defsubst hash-table-values (hash-table) "Return a list of values in HASH-TABLE."
[let (values "(1))
subr-x.el.gz
36% L148 (Emacs-Lisp)  Here goes the editor</div>
    </div>
)
    }
